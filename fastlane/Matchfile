# For more information about Match, see:
#     https://docs.fastlane.tools/actions/match/

# Git repository URL for storing certificates and profiles
# You need to create a private repository for this
git_url("https://github.com/YOUR_USERNAME/certificates.git")

# Storage mode: git or google_cloud
storage_mode("git")

# Default certificate type (development, appstore, adhoc, enterprise)
type("development")

# App identifier(s) to create certificates/profiles for
app_identifier(["com.receiptscanner.app"])

# Your Apple Developer Portal username
# username("your-apple-id@example.com")

# Team ID
# team_id("XXXXXXXXXX")

# Enable readonly mode for CI environments (prevents regenerating certs)
# readonly(true)

# Clone using SSH instead of HTTPS
# git_basic_authorization(Base64.strict_encode64("username:token"))

# Platform (ios, macos, tvos)
platform("ios")
